import { Exercise } from "../models/Exercise"

export function ExercisesFromMongo () {
    // get all exercises that are stored in a particular routine
    // do this instead of mapping from a useState

    return (
        <div>

        </div>
    )
}
